# Sat 29 Aug
I learned about the main tools that will be used to create the project.
Node.js allows JavaScript tools to run on my computer, while npm installs and manages the packages used by the project.
React will be used to build the website with reusable, and TypeScript can help find some errors earlier.
Vite will create initial project and provide a local development server. Later, it will aslo build the website into files that can be deployed to Firebase Hosting.
I also learned that package.json records the project dependencies and commands, while most of my code will be written inside the src folder.